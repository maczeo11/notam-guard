# Runtime
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.11
python-dotenv>=1.0

# Decision graph and tracing
langgraph>=0.2.41
langsmith>=0.2.6

# LLM backend. Imported only when LLM_ADAPTER=groq; the rule adapter needs neither.
langchain-groq>=0.2

# Vector store. Not needed when VECTOR_ADAPTER=memory.
psycopg2-binary>=2.9.10
pgvector>=0.3.6
sentence-transformers>=3.4

# Fleet memory. Falls back to in-process storage when unreachable.
redis>=5.2
